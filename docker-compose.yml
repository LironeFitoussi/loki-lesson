services:
  dev:
    build:
      context: .
      target: dev
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host

  preview:
    build:
      context: .
      target: production
    ports:
      - '8080:80'
